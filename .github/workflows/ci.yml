name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build & Verify (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [20.x]
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Secret-leak audit
        run: npm run lint:secrets

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Coverage (must stay above the per-file threshold)
        run: npm run test:coverage
        # test:coverage uses `c8 --check-coverage` and exits non-zero when
        # any included src/ file falls below 60% stmt / 60% branch / 80% fn.

      - name: Package .vsix
        run: npm run package

      - name: Upload .vsix artifact
        uses: actions/upload-artifact@v4
        with:
          name: minimax.vscode-agent-${{ github.sha }}.vsix
          path: '*.vsix'
          if-no-files-found: warn
